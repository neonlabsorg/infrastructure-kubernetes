apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Release.Namespace }}
  name:  {{ .Values.service.name }}
  labels:
    application-pod: neon-proxy
    {{- include "neon-proxy.labels" . | nindent 4 }}
  annotations:
    cloud.google.com/neg: '{"ingress": true}'    
spec:
  type: {{ .Values.service.type }}
  sessionAffinity: {{ .Values.service.sessionAffinity }}
  clusterIP: {{ .Values.service.clusterIP }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: {{ .Values.service.protocol }}
      name: {{ .Values.service.name }}
  {{- if .Values.extraPorts }}
    {{- range .Values.extraPorts }}
    - name: {{ .name }}
      targetPort: {{ .name }}
      protocol: {{ .protocol }}
      port: {{ .port }}
      {{- if and (eq $.Values.service.type "NodePort") (.nodePort) }}
      nodePort: {{ .nodePort }}
      {{- end }}
    {{- end }}
  {{- end }}      
  selector:
    application-pod: neon-proxy
    {{- include "neon-proxy.selectorLabels" . | nindent 4 }}

---
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ .Values.indexer.service.name }}
  labels:
    application-pod: neon-proxy-indexer
    {{- include "neon-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.indexer.service.type }}
  sessionAffinity: {{ .Values.indexer.service.sessionAffinity }}
  clusterIP: {{ .Values.indexer.service.clusterIP }}
  ports:
    - port: {{ .Values.indexer.service.port }}
      targetPort: {{ .Values.indexer.service.port }}
      protocol: {{ .Values.indexer.service.protocol }}
      name: {{ .Values.indexer.service.name }}
  selector:
    application-pod: neon-proxy-indexer  
    {{- include "neon-proxy.selectorLabels" . | nindent 4 }}

{{- if .Values.coreapi.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ .Values.coreapi.service.name }}
  labels:
    application-pod: neon-core-api
    {{- include "neon-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.coreapi.service.type }}
  sessionAffinity: {{ .Values.coreapi.service.sessionAffinity }}
  clusterIP: {{ .Values.coreapi.service.clusterIP }}
  ports:
    - port: {{ .Values.coreapi.service.port }}
      targetPort: {{ .Values.coreapi.service.port }}
      protocol: {{ .Values.coreapi.service.protocol }}
      name: {{ .Values.coreapi.service.name }}
  selector:
    application-pod: neon-core-api  
    {{- include "neon-proxy.selectorLabels" . | nindent 4 }}
{{- end }}

{{- if .Values.corerpc.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ .Values.corerpc.service.name }}
  labels:
    application-pod: neon-core-rpc
    {{- include "neon-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.corerpc.service.type }}
  sessionAffinity: {{ .Values.corerpc.service.sessionAffinity }}
  clusterIP: {{ .Values.corerpc.service.clusterIP }}
  ports:
    - port: {{ .Values.corerpc.service.port }}
      targetPort: {{ .Values.corerpc.service.port }}
      protocol: {{ .Values.corerpc.service.protocol }}
      name: {{ .Values.corerpc.service.name }}
  selector:
    application-pod: neon-core-rpc
    {{- include "neon-proxy.selectorLabels" . | nindent 4 }}
{{- end }}
